services:
  applio:
    build:
      context: .
      target: runner
    ports:
      - 6969
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: [gpu]
